export type EntryPassData = {
  eventName: string;
  eventDateTime: string;
  entryTag: string;
  valid: boolean;
  ticketId: string;
  locationName: string;
  locationDetail: string;
  qrValue: string;
};

